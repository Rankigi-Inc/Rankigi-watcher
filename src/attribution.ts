/**
 * Phase 2 will parse Error().stack to find a `.obsidian/plugins/<id>/`
 * path segment and return the plugin id. For Phase 1 this is a stub.
 */
export function resolvePluginFromStack(
  _stack: string,
  _pluginsDir: string
): string | null {
  return null;
}
